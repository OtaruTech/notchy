import SwiftUI

@main
struct NotchyApp: App {
    var body: some Scene {
        Settings {
            Text("Notchy settings (placeholder)")
                .padding()
                .frame(width: 360, height: 200)
        }
    }
}
